# Dependencias
node_modules/

# Variables de entorno (contienen credenciales reales)
.env
.env.local
backend/.env
backend/.env.local
backend-php/public/api/config.php

# Builds
frontend/dist/
backend/public/
backend-php/public/assets/
backend-php/public/index.html
backend-php/public/icons.svg
backend-php/public/logo-anden.png

# Archivos subidos por usuarios (PDFs de romaneaje) — no versionar
backend/uploads/
backend-php/uploads/

# Logs
*.log
npm-debug.log*

# Editor / SO
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Empaquetado de assets (ya vive descomprimido en assets/)
assets.zip

# Herramientas del agente (no es parte del proyecto)
/.claude/

# Snapshot viejo en la raíz (previo a la reestructura backend/ + frontend/,
# incluye un build ya compilado y rutas ya renombradas como bajas.js) — se
# deja en disco por si sirve de referencia, pero no se versiona para no
# confundir con el código activo.
/src/
/public/
/setup.js
/package.json
/package-lock.json
/index.html
/icons.svg
/logo-anden.png
/assets/
/schema.sql
/sistemas_desarrollo/

# Paquetes de despliegue generados (zip del backend, SQL de producción)
/deploy/
